{
  "name": "cancurrently-workspace",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "test": "cd tests/integration && npm test",
    "test:watch": "cd tests/integration && npm run test:watch"
  }
}
